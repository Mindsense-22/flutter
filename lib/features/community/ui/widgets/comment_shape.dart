import 'package:flutter/material.dart';

class CommentShape extends StatelessWidget {
  const CommentShape({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      decoration: BoxDecoration(

      ),
      child: Row(
        children: [
          
        ],
      ),
    );
  }
}